<Window x:Class="Shared.Templates.Widget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:models="clr-namespace:Shared.Models"
        ShowInTaskbar="False" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Left="{Binding Left}" Top="{Binding Top}"
        Width="{Binding Width}" Height="{Binding Height}"
        MinWidth="{Binding MinSize}" MinHeight="{Binding MinSize}"
        d:DataContext="{d:DesignInstance Type=models:WidgetViewModel}" mc:Ignorable="d">
    <Border Background="{Binding Background}" CornerRadius="{Binding CornerRadius}" Padding="{Binding Padding}">
        <ContentPresenter Content="{Binding Control}" />
    </Border>
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="{Binding Locale.Edit}" Command="{Binding ContextMenu.EditWidget}" />
            <Separator />
            <MenuItem Header="{Binding Locale.Size}" IsEnabled="False" />
            <MenuItem Header="{Binding Locale.Small}" Command="{Binding ContextMenu.Small}" />
            <MenuItem Header="{Binding Locale.Medium}" Command="{Binding ContextMenu.Medium}" />
            <MenuItem Header="{Binding Locale.Large}" Command="{Binding ContextMenu.Large}" />
            <Separator />
            <MenuItem Header="{Binding Locale.RemoveWidget}" Command="{Binding ContextMenu.RemoveWidget}" />
            <Separator />
            <MenuItem Header="{Binding Locale.EditWidgets}" Command="{Binding ContextMenu.EditWidgets}" />
            <Separator />
            <MenuItem Header="Light Theme" Command="{Binding ContextMenu.LightMode}" />
            <MenuItem Header="Dark Theme" Command="{Binding ContextMenu.DarkMode}" />
        </ContextMenu>
    </Window.ContextMenu>
</Window>
